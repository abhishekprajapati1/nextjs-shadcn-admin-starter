import React from "react";
import { Button } from "../ui/button";

const PurchaseFrameOnly = () => {
  return (
    <React.Fragment>
      <Button variant="secondary" title="Buy only frame excluding lenses">
        Frame Only
      </Button>
    </React.Fragment>
  );
};
export default PurchaseFrameOnly;
